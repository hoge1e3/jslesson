define('__main__',function (require,exports,module) {
  var PYLIB=require('http://localhost/runtime/lib/python/PyLib.js');
  var range=PYLIB.range;
  var input=PYLIB.input;
  var str=PYLIB.str;
  var int=PYLIB.int;
  var float=PYLIB.float;
  var len=PYLIB.len;
  var type=PYLIB.type;
  var quit=PYLIB.quit;
  var exit=PYLIB.exit;
  var sorted=PYLIB.sorted;
  var fillRect=PYLIB.fillRect;
  var setColor=PYLIB.setColor;
  var setTimeout=PYLIB.setTimeout;
  var clearRect=PYLIB.clearRect;
  var clear=PYLIB.clear;
  var open=PYLIB.open;
  PYLIB.LoopChecker.reset();
  var plt=require('http://localhost/runtime/lib/python/py_matplotlib,pyplot.js').install(PYLIB);
  var riritsu=0.05;
  var yokin=[100000];
  var i;
  for (i of range(10)) {
    PYLIB.LoopChecker.check();
    var risoku=int(PYLIB.wrap(yokin[i]).__mul__(riritsu));
    yokin.append(PYLIB.wrap(yokin[i]).__add__(risoku));
  }plt.title("FUKURI KEISAN");
  plt.xlabel("Year");
  plt.ylabel("Yokin[YEN]");
  plt.plot(yokin,PYLIB.opt({marker:"o"}));
  plt.show();
});
requirejs(['__main__'],function(){});
