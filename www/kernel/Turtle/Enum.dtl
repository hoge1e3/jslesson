// Dolittle
module ! "Vec2" "Actor" "UI" "Turtle" "Figure" "DOM" "Japanese"  
[
    window:parent:root = root.
    console ! (window:parent) log.
    console ! (root) log.
] require.
/*
実行後、↓をコンソールで実行
for (var k in root) {
    o=root[k];
    console.log(k);
    for (var k2 in o) {
        if (!o.hasOwnProperty(k2)) continue;
        console.log(k+":"+k2);
    }
}
*/
