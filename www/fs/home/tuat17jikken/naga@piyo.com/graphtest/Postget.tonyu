native window;
onClick("b",send);
v=getFromServer("testkey");
setText("a",v);
function send(){
    t=getText("t");
    putToServer("testkey",t);
    v=getFromServer("testkey");
    setText("a",v);
}


\putToServer(key,value) {
    var url=window.location.href;
    var p=window.$.ajax({
        url: window.runtimePath+"a.php?KeyValue/put",
        type:'POST',
        data : {key : key, value : value, url : url, group : activityGroup}
    });
    var r;
    r=waitFor(p);
    return r;
}