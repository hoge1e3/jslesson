// JavaScript
// ここで扱われるJavaScriptは通常のJavaScriptとは異なります。詳しくは使用方法をご覧ください。
onClick("b",judge);

function judge(){
    jcode=rnd(3);
    //setText("j",jcode);
    if(jcode==0){
        setText("j","ぐー"); 
    }else if(jcode==1){setText("j","ちょき"); 
        
    }else if(jcode==2){setText("j","ぱー"); 
        
    }
}
// JavaScript
// ここで扱われるJavaScriptは通常のJavaScriptとは異なります。詳しくは使用方法をご覧く