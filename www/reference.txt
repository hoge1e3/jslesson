addText(名前,値または文字列)
setText(名前,値または文字列)
getText(名前)
getNumber(名前)
setNumber(名前,数値)
onClick(名前,命令名)

setCanvas(名前)
setColor(赤,緑,青)
fillRect(横,縦,幅,高さ)
setBGColor(色)

changeImage(名前,画像アドレス)
move(名前, 横 ,縦)
transform(名前 ,角度, 縦比率, 横比率)
rotate(名前,角度)
resize(名前 , 縦比率, 横比率)
wait(時間)
rnd(値)
